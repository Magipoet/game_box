# 记忆翻牌游戏 — 问题记录

## 问题 1：关卡信息布局过宽

### 现象
"第1关 (3×2)" 这行信息显示在关卡按钮的右侧，导致游戏界面整体过宽，布局不紧凑。

### 原因
关卡标签（`currentLevelLabel`）与关卡按钮（`levelBtn`）在同一行水平排列，关卡信息作为按钮的相邻元素显示在右侧，而非垂直排列在下方。

### 解决方案
将关卡信息区域用 `.level-info` 容器包裹，改为垂直布局（flex-direction: column），使关卡标签和星级显示位于关卡按钮的下方。同时添加响应式样式，在小屏幕上自动调整间距和字体大小。

**涉及文件**：`index.html`、`css/header.css`

---

## 问题 2：限时模式缺少暂停功能

### 现象
限时模式下没有暂停按键，玩家无法在中途暂停游戏；且游戏开始前不显示剩余时间，只有点击开始后才显示倒计时。

### 原因
原始实现中限时模式仅设计了一个计时器，没有考虑暂停需求；倒计时显示逻辑仅在游戏开始后（`startCountdown`）才渲染，游戏重置时未预先展示关卡时限。

### 解决方案
1. 在页面头部添加暂停按钮（仅在限时模式下显示）
2. 添加暂停模态框，显示当前步数和配对进度，提供"继续游戏"按钮
3. 修改 `resetStats()` 函数，在限时模式重置时立即显示该关卡的剩余时间
4. 新增 `pauseGame()`、`resumeGame()`、`hidePauseModal()` 函数，暂停时同时停止正计时和倒计时

**涉及文件**：`index.html`、`css/header.css`、`js/state.js`、`js/game.js`、`js/events.js`

---

## 问题 3：关卡标签中多余的计时信息

### 现象
- 常规模式下，关卡标签显示 `第1关 (3×2) ⏱️60秒`，其中的"⏱️60秒"在常规模式下是多余信息
- 限时模式下，关卡标签同样显示 `⏱️60秒`，但标题下方已有独立的倒计时显示区域，造成信息重复

### 原因
`updateDifficultyButtons()` 函数中通过 `modeText` 变量在限时模式下追加 `⏱️{timeLimit}秒` 文本，而该信息在两种模式下均属冗余——常规模式无需时间提示，限时模式已有专用倒计时区域。

### 解决方案
删除 `modeText` 变量及其在 `currentLevelLabel.textContent` 中的引用，关卡标签统一只显示 `第{N}关 ({cols}×{rows})` 格式。

**涉及文件**：`js/game.js`（`updateDifficultyButtons` 函数）

---

## 问题 4：常规模式与限时模式的星级和通关进度未分离

### 现象
常规模式通关某关卡后，切换到限时模式时，该关卡也被标记为已通关并获得了星级，两种模式的进度混为一体。

### 原因
星级数据仅使用单一的 `levelStars` 对象存储，`saveStars()` 和 `getTotalStars()` 函数不区分游戏模式，导致两种模式共享同一份星级记录。关卡解锁也仅使用单一的 `unlockedLevels` 对象，常规模式通关解锁的关卡自动在限时模式也可见。

### 解决方案
1. 新增 `timedLevelStars` 对象存储限时模式独立星级，新增 `STORAGE_KEYS.timedStars` 存储键
2. 新增 `unlockedTimedLevels` 对象存储限时模式独立解锁状态，新增 `STORAGE_KEYS.timedProgress` 存储键
3. 修改 `saveStars()` / `getStarsForLevel()` / `getTotalStars()` 函数，增加 `mode` 参数按模式分别存取
4. 新增 `unlockNextTimedLevel()` / `saveTimedProgress()` / `getUnlockedLevelsForMode()` 函数
5. 修改 `showWinModal()`，通关时根据当前模式分别保存星级和解锁下一关
6. 修改 `updateDifficultyButtons()` 和 `updateCurrentLevelStars()`，显示当前模式的星级和解锁状态
7. 修改设置面板中的记录显示，同时展示两种模式的星级并标注"常规"/"限时"
8. 修改成就检查中的星级统计，区分两种模式

**涉及文件**：`js/config.js`、`js/state.js`、`js/storage.js`、`js/game.js`、`js/settings.js`、`js/achievements.js`、`css/settings.css`

---

## 问题 5：关卡下拉框中两种模式的关卡混合展示

### 现象
点击关卡按钮后，常规模式和限时模式的关卡混在同一个列表中展示，没有区分。玩家无法直观看出哪些关卡是常规模式进度、哪些是限时模式进度。

### 原因
`updateDifficultyButtons()` 函数仅生成一个关卡列表，虽然已根据 `gameMode` 读取对应模式的星级和解锁状态，但两种模式没有在视觉上分离展示，且切换模式查看进度需要先切换游戏模式再打开下拉框。

### 解决方案
1. 在关卡下拉框顶部添加"常规"和"限时"两个标签页（tab），各显示对应模式的总星星数
2. 新增 `levelDropdownActiveTab` 变量跟踪当前查看的标签页
3. 点击标签页时刷新关卡列表，显示对应模式的星级和解锁状态
4. 跨模式选关时自动切换游戏模式（有未完成进度时需确认）
5. 打开下拉框时默认对齐当前游戏模式的标签页
6. 新增标签页相关 CSS 样式（含暗黑模式适配）

**涉及文件**：`js/game.js`（`updateDifficultyButtons`、`toggleLevelDropdown`）、`css/header.css`

---

## 问题 6：切换模式后当前关卡未重置，导致严重的逻辑错误

### 现象
常规模式通过第5关后到达第6关，此时切换到限时模式，游戏仍然停留在第6关，而限时模式可能仅解锁到第2关。玩家可以直接操作未解锁的限时模式关卡，严重违反游戏规则。

### 原因
`currentLevel` 是全局共享的单一变量，常规模式和限时模式共用。之前的修改实现了独立的解锁状态（`unlockedLevels` / `unlockedTimedLevels`）和独立的星级记录（`levelStars` / `timedLevelStars`），但**没有维护各自独立的当前关卡记录**。

切换模式时，`setGameMode()` 仅修改了 `gameMode` 并调用 `restartGame()`，但没有将 `currentLevel` 重置为新模式的实际进度，导致关卡号被"继承"到新模式中。

### 解决方案
1. 新增 `STORAGE_KEYS.normalCurrentLevel` 和 `STORAGE_KEYS.timedCurrentLevel` 存储键，分别保存两种模式的当前关卡
2. 新增 `saveCurrentLevelForMode(level, mode)` 函数，按模式分别持久化当前关卡号
3. 新增 `getFirstUnlockedLevel(unlockedObj)` 辅助函数，查找某模式下第一个已解锁的关卡
4. 修改 `loadGameData()`，初始化时根据当前模式加载对应的保存关卡，并校验该关卡是否已解锁
5. 修改 `setGameMode()`，切换模式时：
   - 先保存旧模式的当前关卡（`saveCurrentLevelForMode(currentLevel, oldMode)`）
   - 切换模式后，读取新模式的保存关卡作为 `currentLevel`
   - 校验目标关卡在新模式下是否已解锁，未解锁则回退到第一个已解锁关卡
6. 修改 `nextLevel()`、`changeLevel()` 和下拉框跨模式选关逻辑，关卡变化时同步调用 `saveCurrentLevelForMode()` 持久化

**涉及文件**：`js/config.js`、`js/storage.js`、`js/game.js`

---

## 问题 7：教程炸弹介绍卡片与教程指南卡片重叠遮挡

### 现象
在教程的炸弹介绍界面，`tutorial-bomb-modal`（炸弹介绍弹窗）与底部 `.tutorial-guide-content`（教程指南卡片）同时显示，产生视觉重叠遮挡，严重影响观感。

### 原因
教程进行到炸弹演示环节时，`bombExplode()` 函数显示炸弹弹窗（`tutorial-bomb-modal`），但没有隐藏底部的教程指南内容。由于 `.tutorial-guide-content` 的 `z-index: 1501` 高于 `.modal` 的 `z-index: 1000`，教程指南卡片会覆盖在炸弹弹窗上方，造成遮挡。

### 解决方案
1. 在 `bombExplode()` 函数中，显示炸弹弹窗时同步隐藏 `.tutorial-guide-content`（添加 `hidden` 类）
2. 在 `tutorialBombCloseBtn` 的点击处理函数和点击弹窗外部关闭的处理函数中，关闭弹窗时同步显示 `.tutorial-guide-content`（移除 `hidden` 类）
3. 在 `css/tutorial.css` 中添加 `.tutorial-guide-content.hidden { display: none; }` 样式规则

**涉及文件**：`js/game.js`、`js/events.js`、`css/tutorial.css`

---

## 问题 8：限时模式下启动教程，教程在限时模式中进行且计时开始

### 现象
如果在限时模式下点击"开始新手教程"，教程会在限时模式下进行，游戏倒计时会同时启动，不符合教程应在常规模式下进行的设计要求。

### 原因
`startTutorial()` 函数启动教程时，没有将游戏模式切换回常规模式，也没有停止倒计时，导致教程继承了当前的限时模式状态。

### 解决方案
在 `startTutorial()` 函数开头添加模式切换和计时器清理逻辑：
1. 将 `gameMode` 设为 `GAME_MODES.NORMAL`
2. 调用 `saveGameMode()` 持久化
3. 调用 `stopCountdown()` 停止倒计时
4. 调用 `updateModeButtons()` 更新UI按钮状态

同时在 `endTutorial()` 函数中添加相同的清理逻辑，确保教程结束后游戏回到常规模式。

**涉及文件**：`js/tutorial.js`

---

## 问题 9：教程缺少限时模式介绍

### 现象
教程步骤中只介绍了基本的翻牌配对、配对失败和炸弹机制，完全没有提及限时模式这一重要游戏特性，新手玩家无法从教程中了解到限时模式的存在和玩法。

### 原因
`tutorialSteps` 数组中没有限时模式相关的介绍步骤。

### 解决方案
在 `tutorialSteps` 数组中添加限时模式介绍步骤（位于炸弹演示之后、"开始游戏"之前），包含：
1. 限时模式规则文字说明
2. 交互式引导：高亮"限时"按钮，指引玩家点击切换模式
3. 交互式引导：切换到限时模式后，高亮卡片，指引玩家点击观察倒计时运行
4. 演示完成后自动切回常规模式

新增状态变量 `tutorialTimedModeActive`、`tutorialTimedCardClicked`、`tutorialTimedDemoTimer`，新增函数 `handleTutorialTimedModeClick()`、`handleTutorialTimedCardClick()`、`handleTutorialTimedNextClick()`、`completeTimedModeDemo()`、`clearTutorialTimedDemoTimer()`。

**涉及文件**：`js/tutorial.js`、`js/events.js`、`js/state.js`、`index.html`

---

## 问题 10：配对失败教程步骤存在明显停滞感

### 现象
教程进行到"配对失败会怎样"步骤时，展示配对失败结果后需要玩家手动点击"下一步"才能进入炸弹模块介绍，交互不够流畅，存在明显的停滞感。

### 原因
步骤5的 `action` 为 `show-mismatch-result`，只显示文字说明，没有自动前进机制，用户必须手动点击"下一步"按钮。

### 解决方案
在 `executeTutorialAction()` 函数中，为 `show-mismatch-result` 动作添加2.5秒的自动前进定时器：
- 进入该步骤时设置 `tutorialAutoAdvanceTimer`
- 2.5秒后自动前进到步骤6（炸弹介绍）
- 自动前进前检查当前仍在教程且仍处于该步骤
- 新增 `clearTutorialAutoAdvanceTimer()` 函数用于清理定时器
- 在 `updateTutorialStep()`、`startTutorial()`、`endTutorial()` 及所有重置函数中调用清理函数

**涉及文件**：`js/tutorial.js`、`js/state.js`

---

## 问题 11：限时模式教程步骤中卡片自动翻开（严重逻辑错误）

### 现象
教程进行到"限时模式体验（一）"步骤时，指引玩家点击限时模式按钮，但点击后棋盘上有一张牌被自动翻开，玩家还没有进行任何操作。同时教程卡在这一步，点击"下一步"按钮也没有任何反应。

### 原因
这是一个**事件冒泡 + 状态竞争**的严重逻辑bug，完整触发链如下：

1. 用户在步骤8点击「限时」按钮
2. `modeTimedBtn` 点击处理函数执行，调用 `handleTutorialTimedModeClick()`
3. `handleTutorialTimedModeClick()` 内部调用 `updateTutorialStep(9)`，此时 `currentTutorialStep` 已变为 9
4. **但点击事件还在继续向上冒泡**
5. 事件冒泡到 `document` 的全局点击处理器
6. 全局处理器读取 `step = tutorialSteps[currentTutorialStep]`，此时已经是步骤9（`wait-timed-card-click`）
7. 步骤9的 `showNext = true`，且 `action` 不是 `free-play`
8. 全局处理器检查点击目标是否在排除列表中——模式按钮（`.mode-btn`）**不在排除列表中**
9. 所有检查通过，自动执行 `tutorialNextBtn.click()`
10. 触发步骤9的下一步处理函数，调用 `autoFlipCard()` 自动翻牌

此外，即使卡片已翻转，`autoFlipCard()` 会因 `card.flipped` 检查提前返回，导致 `completeTimedModeDemo()` 永远不会被调用，教程卡住。

### 解决方案
采用**双重保险**机制彻底解决：

1. **修复1：在全局点击处理器中排除模式按钮**
   - 在 `document.addEventListener('click', ...)` 的教程处理逻辑中添加 `if (e.target.closest('.mode-btn')) return;`

2. **修复2：阻止模式按钮点击事件冒泡**
   - 在 `modeNormalBtn` 和 `modeTimedBtn` 的点击处理函数中添加 `e.stopPropagation()`，阻止事件冒泡到 `document`

3. **修复3：完整重置卡牌和游戏状态**
   - 在 `handleTutorialTimedModeClick()` 中添加完整的卡牌翻回和游戏状态重置逻辑，确保进入步骤9时所有卡片都是未翻转状态

4. **修复4：处理卡牌已翻转的边缘情况**
   - 新增 `handleTutorialTimedNextClick()` 函数，处理步骤9的"下一步"按钮点击，能正确处理卡牌已翻转的情况
   - 更新 `handleTutorialTimedCardClick()` 函数，当卡牌已翻转时直接启动游戏并调用 `completeTimedModeDemo()`

**涉及文件**：`js/events.js`、`js/tutorial.js`

---

## 问题 12：游戏首页直接进入游戏，缺少关卡选择界面

### 现象
打开游戏首页就是游戏具体内容，玩家直接进入当前关卡开始游戏，无法查看整体关卡地图和进度。缺少类似开心消消乐的关卡选择界面，即先展示所有关卡及其解锁/星级状态，玩家点击某一关卡后才正式进入游戏界面。

### 原因
`index.html` 中仅包含游戏界面容器（`game-screen`），没有独立的关卡选择页面容器。`initGame()` 函数启动时直接调用 `createBoard()` 渲染游戏棋盘，默认进入游戏状态，没有提供关卡选择的 UI 和交互流程。

### 解决方案
1. 在 `index.html` 中新增 `game-screen` 之前新增 `.level-select-screen` 容器，包含：游戏标题栏、模式切换标签、总星星显示、关卡地图区域、游戏界面新增"关卡选择"返回按钮
2. 新增 `css/level-select.css` 样式文件，实现关卡节点、关卡节点样式、星星显示、锁定状态、当前关卡高亮等
3. 新增 `showLevelSelectScreen()` / `showGameScreen()` 页面切换函数，通过添加/移除 `hidden` 类切换两个界面的显示
4. 新增 `renderLevelSelectMap()` 函数，根据当前模式渲染关卡地图，计算每个关卡的解锁状态、当前关卡、星级显示
5. 新增 `enterLevel(levelIndex, mode)` 函数，点击关卡节点后切换到游戏界面并初始化对应关卡
6. 修改 `initGame()` 函数，启动时默认显示关卡选择页面而非直接进入游戏
7. 修改教程 `startTutorial()`，首次进入关卡时自动切换到游戏界面
8. 在 `showWinModal()` 中新增"选择关卡"按钮，点击后返回关卡选择页面
9. 修改 `events.js`，新增模式切换、返回关卡选择等按钮事件处理

**涉及文件**：`index.html`、`css/level-select.css`、`js/game.js`、`js/events.js`、`js/tutorial.js`

---

## 问题 13：关卡选择界面使用规整网格，缺少路径设计感

### 现象
关卡选择页面中所有关卡整齐排列成一排一排的规整网格，类似表格布局，没有任何设计感。缺少开心消消乐那种关卡沿着蜿蜒路径（藤蔓/地图）向下延伸的视觉效果，只有单调的行列排列。

### 原因
`css/level-select.css` 中使用 `grid-template-columns: repeat(4, 1fr)` 的 CSS Grid 规整网格布局，所有关卡按固定行列对齐，没有路径连线和装饰元素。`renderLevelSelectMap()` 只生成一个个独立的卡片节点，没有计算位置偏移和连线逻辑。

### 解决方案
1. **完全重写关卡布局**：将 CSS Grid 布局改为 `position: absolute` 绝对定位，每个关卡节点独立设置 `left` 百分比值 + `top` 像素值进行精确定位
2. **SVG 路径连线**：在 `.level-select-map` 中添加 SVG 画布，使用三次贝塞尔曲线（`C` 命令）绘制贯穿所有关卡的绿色渐变藤蔓路径，包含半透明阴影底衬和渐变主线两层
3. **蜿蜒排列算法**：使用 `i % 4` 循环分配水平位置，形成「左 → 中 → 右 → 中 的蜿蜒路径
4. **装饰元素**：在地图两侧随机分布 12 个装饰表情符号（🌿🍃🌸✨🌟☁️🦋），部分元素带浮动/闪烁动画，随机动画延迟
5. **视觉升级**：关卡节点增加立体光泽效果（顶部高光、底部内阴影，当前关卡金色脉冲光环动画
6. **响应式适配**：600px / 400px 两个移动端断点，调整节点大小和水平位置
7. **暗黑模式适配**：同步更新 tooltip 箭头颜色、文本颜色

**涉及文件**：`css/level-select.css`、`js/game.js`（`renderLevelSelectMap` 函数）

---

## 问题 14：路径连线节点分布逻辑不一致（部分连线上 2 个节点，部分 3 个节点）

### 现象
关卡选择页面的路径上，从第 2 关到第 4 关的连线中间经过第 3 关，视觉上有 3 个节点，而从第 1 关到第 2 关只有 2 个节点，节点分布不一致，明显存在逻辑错误。

### 原因
位置循环使用 `i % 3`：左→中→右→左→中→右... 的循环：
- i=0（第1关）→ 左（第2关）→ 中（第3关）→ 中（第4关）

第 1→2→3→4 的视觉上：
- 第 1→2 只是半个 S 形（左→中），只有两个端点
- 第 2→3→4 是一个完整 S 形（中→右→中），中间包含 3 个点
贝塞尔曲线控制点使用相同的中点 Y 值，使相邻两条小 S 曲线视觉上合并成一条大 S 曲线。

### 解决方案
1. **修改位置循环模数**：将水平位置循环从 `i % 3` 改为 `i % 4`，模式改为「左(22%) → 中(50%) → 右(78%) → 中(50%) → ...」
2. **每 3 个相邻节点恰好形成一个完整 S 形**：
   - 第 1→2→3：左→中→右（完整 S 形，3 个节点）
   - 第 3→4→5：右→中→左（完整 S 形，3 个节点）
   - 第 5→6→7：左→中→右（完整 S 形，3 个节点）
3. **CSS 类更新**：`.level-pos-center` 改为 `.level-pos-middle`，同步更新 tooltip 位置、暗黑模式下的箭头颜色
4. **JS 类名分配**：`posInGroup === 0 → level-pos-left`，`posInGroup === 2 → level-pos-right`，`posInGroup === 1 或 3 → level-pos-middle`

**涉及文件**：`js/game.js`（`renderLevelSelectMap` 函数）、`css/level-select.css`

---

## 问题 15：教程回退到"限时模式体验（二）时卡片未重置导致教程卡死

### 现象
完成「限时模式体验（二）步骤（引导点击高亮卡片观察倒计时运行）→ 进入下一步「开始游戏！」→ 点击「上一步」回退到该步骤时，之前用户点击后已翻开的卡片没有回到背面状态，卡片仍然保持翻开，导致再次点击该卡片时翻牌检查（`card.flipped` 为 true），`flipCard` 函数直接 `return`，无任何反应，教程彻底卡住。

### 原因
1. `tutorialPrevBtn` 处理中从步骤 10 回退到步骤 9 时，仅设置了 `currentTutorialStep = 9`，未调用任何状态重置函数
2. `executeTutorialAction` 的 `wait-timed-card-click` 动作只负责高亮卡片和显示箭头，未检查并重置卡片状态
3. `flipCard` 函数开头有 `if (lockBoard || card.matched || card.flipped || ...) return;` 的前置检查，已翻开的卡片无法再次点击触发

### 解决方案
1. **新增轻量级重置函数** `resetTutorialCardOnly()`：只重置卡片 `flipped`/`matched` 状态和游戏统计（moves/seconds/firstCard 等），但不修改 `tutorialTimedModeActive`、`tutorialTimedCardClicked` 等教程标记位，避免影响教程流程
2. **修改步骤切换动作** `executeTutorialAction` 的 `wait-timed-card-click`：调用 `resetTutorialCardOnly()` 重置卡片
3. **修改回退逻辑** `tutorialPrevBtn` 处理中步骤 10 → 步骤 9：
   - 调用 `resetTutorialCardOnly()` 重置卡片
   - 显式设置 `gameMode = GAME_MODES.TIMED`，保存并更新按钮状态
   - 恢复 `timeRemaining = level.timeLimit`，更新倒计时显示

**涉及文件**：`js/tutorial.js`、`js/events.js`

---

## 问题 16：教程回退到「限时模式体验（二）后自动推进失效，彻底卡住

### 现象
回退到「限时模式体验（二）时，界面一直停在这一步，既不会过一会儿自动进行到下一步，手动点击高亮卡片也没有任何反应，教程彻底卡住。用户体验极差。

### 原因
`completeTimedModeDemo()` 函数内部有一个关键条件检查 `if (isTutorialActive && tutorialTimedModeActive) { ... }`，只有当 `tutorialTimedModeActive = true` 时才会在 3.5 秒后执行推进逻辑（恢复常规模式并进入步骤 10）。而从步骤 10 结束时会将 `tutorialTimedModeActive = false`，回退到步骤 9 时未重新设置为 true，导致：
1. 用户点击卡片后 `completeTimedModeDemo()` 被调用，但条件不满足，空转
2. 3.5 秒自动推进的定时器什么都不做
3. 用户点击「下一步」按钮时调用 `handleTutorialTimedNextClick()`，即使卡片已翻转也能启动倒计时，但仍然调用 `completeTimedModeDemo()` 同样因条件不满足

### 解决方案
在 `executeTutorialAction` 的 `wait-timed-card-click` 动作中，**完整初始化该步骤所需的所有状态**：
1. `tutorialTimedModeActive = true` —— 确保 `completeTimedModeDemo()` 的条件满足，能执行推进逻辑
2. `tutorialTimedCardClicked = false` —— 重置点击状态
3. `clearTutorialTimedDemoTimer()` —— 清除可能存在的旧定时器，避免状态竞争
4. `resetTutorialCardOnly()` —— 重置卡片和游戏状态
5. 恢复 `timeRemaining` 为关卡初始时间，更新倒计时显示
6. 显式设置 `gameMode = GAME_MODES.TIMED` 并更新 UI 按钮状态
7. 高亮目标卡片并显示箭头引导

**涉及文件**：`js/tutorial.js`（`executeTutorialAction` 函数）

---

## 问题 17：限时模式教程步骤强制等待，点击"下一步"无效

### 现象
教程进行到"限时模式体验（二）"时，顶部显示倒计时栏后，必须强制等待几秒钟才会进入下一个部分。点击"下一步"按钮没有任何效果，设计不符合逻辑。

### 原因
`completeTimedModeDemo()` 函数内部固定使用 `setTimeout` 延迟 3500ms 才执行推进逻辑，且 `handleTutorialTimedNextClick()` 函数没有提供立即跳过的路径，无论用户是否点击"下一步"按钮，都必须等待定时器结束。

### 解决方案
1. 为 `completeTimedModeDemo()` 函数新增 `immediate` 参数，当传入 `true` 时立即执行推进逻辑而不等待定时器
2. 修改 `handleTutorialTimedNextClick()` 函数，点击"下一步"时调用 `completeTimedModeDemo(true)` 立即跳过等待
3. 用户通过正常点击卡片体验限时模式效果时，仍保留 3.5 秒的等待延迟

**涉及文件**：`js/tutorial.js`（`completeTimedModeDemo`、`handleTutorialTimedNextClick` 函数）

---

## 问题 18：关卡连线斜率方向修改错误，连线反而更陡

### 现象
要求"减小关卡之间连线的斜率，让连线更平缓"，但修改后连线几乎成了竖直线，观感相当糟糕，斜率反而比原来更大了。

### 原因
对斜率的概念理解错误。斜率 = 纵向变化量 / 横向变化量，要**减小斜率**（更平缓）需要**增大横向距离**，即让左右节点更靠近两侧。修改时反而将节点往中间靠拢（从 30%/70% 改到 40%/60%），横向距离从 40% 缩小到 20%，斜率反而增大了一倍。

### 解决方案
1. 将关卡左右位置从 40%/60% 改为 12%/88%（移动端 10%/90%），横向跨度从 20% 增大到 76%，斜率减小约 73%
2. 同步调整装饰元素的位置范围（从 5-20% / 75-90% 调整为 2-10% / 90-98%），保持与路径节点的相对位置

**涉及文件**：`js/game.js`（`renderLevelSelectMap` 函数、装饰元素位置计算）、`css/level-select.css`

---

## 问题 19：体力条设计过于复杂，布局位置不合理

### 现象
1. 体力条包含橙色渐变背景、闪电图标、黄色进度条和数字文本，结构复杂占用空间过大
2. 星星数量（⭐12）和体力条显示在标题下方独立居中区域，而非标题左侧
3. 标题"记忆配对"位置不居中，视觉偏右影响观感

### 原因
1. 体力条UI设计过度，保留了不必要的黄色进度条区域
2. 星星和体力条在HTML结构中放在独立的 `level-select-stats` 区域（标题下方），而非 header 内
3. 标题使用 `flex: 1` 配合 `text-align: center`，居中是相对于"剩余空间"而非整个容器，左侧星星+体力条占据空间后标题视觉偏右

### 解决方案
1. **简化体力条UI**：删除 `stamina-bar` 和 `stamina-fill` 黄色进度条元素，只保留闪电图标（⚡）和数字文本（如 "30/30"）
2. **调整布局结构**：在 `level-select-header` 内新增 `level-select-header-left` 容器，将星星和体力条从独立区域移到标题左侧
3. **标题绝对居中**：将 `.level-select-title` 从 `flex: 1` 改为 `position: absolute; left: 50%; top: 50%; transform: translate(-50%, -50%)`，实现相对于整个 header 容器的精确居中，不受左右元素宽度影响
4. 同步更新 `updateStaminaDisplay()` 函数，移除对已删除元素的引用

**涉及文件**：`index.html`、`css/level-select.css`、`css/header.css`、`js/storage.js`（`updateStaminaDisplay` 函数）、`js/game.js`

---

## 问题 20：星星数字位数变化导致体力条位置左右抖动

### 现象
切换游戏模式（如从常规模式切换到限时模式）时，星星数量可能从一位数（如6）变为两位数（如12），导致星星模块宽度变化，相邻的体力条模块位置发生左右偏移，观感非常糟糕。

### 原因
星星数量 `.stars-count` 的宽度由内容决定（`width: auto`），数字位数变化时模块整体宽度随之变化，而体力条作为相邻的 inline-flex 元素，位置会被推动。

### 解决方案
1. `.level-select-total-stars .stars-count` 设置 `min-width: 3ch`（三位数固定宽度，1ch = 字符'0'的宽度）+ `text-align: center`
2. `.level-select-stamina .stamina-text` 同样设置 `min-width: 5ch`（如 "30/30" 五位字符宽度），保持体力条数字宽度也固定
3. 游戏界面头部的 `.stat-item.game-stamina .stamina-text` 同步设置 `min-width: 5ch`
4. 桌面端和移动端响应式样式同步更新

**涉及文件**：`css/level-select.css`、`css/header.css`

---

## 问题 21：教程中点击"关卡选择"按钮导致状态不一致

### 现象
打开新手教程后，点击界面左上角的"关卡选择"按钮，会直接回到关卡选择界面，但教程还在继续运行，教程提示框仍然显示，造成状态混乱。

### 原因
"关卡选择"按钮（`backToLevelsBtn`）的点击事件直接调用 `showLevelSelectScreen()` 切换页面，但没有检查教程是否正在进行。教程系统使用 `isTutorialActive` 标志位跟踪状态，而按钮点击事件和 `showLevelSelectScreen()` 函数都没有对该标志位做判断，导致教程进行时也可以被打断。

### 解决方案
采用**双重拦截**机制：
1. **第一层拦截**：在 `backToLevelsBtn` 点击事件最前面添加判断，如果 `isTutorialActive === true` 则直接 `return`，不弹出任何对话框
2. **第二层拦截**：在 `showLevelSelectScreen()` 函数开头添加同样的判断，防止其他调用路径（如 `selectLevel()`）绕过事件处理直接调用

教程正常退出机制：用户完成所有教程步骤后通过 `endTutorial()` 函数自动将 `isTutorialActive` 设为 `false` 并重启游戏。

**涉及文件**：`js/events.js`（`backToLevelsBtn` 点击事件）、`js/game.js`（`showLevelSelectScreen` 函数）

---

## 问题 22："返回当前关"按钮定位基准错误，位于整个屏幕的左边而非白色内容区域内

### 现象
"返回当前关"按钮出现在屏幕最左侧的紫色背景区域，而不是关卡选择界面白色内容区域内部的左边。在宽屏设备上，按钮明显位于白色卡片区域之外，视觉上与内容脱节。

### 原因
`.scroll-to-current-btn` 使用 `position: fixed; left: 16px; top: 50%;` 定位，定位基准是整个浏览器视口（viewport），而非白色内容区域（`.level-select-screen`）。在宽屏设备上，视口左边缘到白色内容区域左边缘之间存在紫色背景区域，导致按钮落在白色区域之外。

页面布局结构：
- 紫色背景 → `body`（全屏）
- 白色内容区域 → `.level-select-screen`（`max-width: 500px`，居中）
- 按钮 → `position: fixed` 基于视口，`left: 16px` 距离视口左边

### 解决方案
1. 将 `.scroll-to-current-btn` 的定位方式从 `position: fixed` 改为 `position: absolute`
2. 确认父容器 `.level-select-screen` 已设置 `position: relative`（已满足）
3. 调整 `left` 值，使按钮位于白色内容区域内部的左侧边缘（如 `left: calc(-1 * (100vw - 100%) / 2 - 60px);` 或使用固定负值配合 transform）
4. 或使用 `position: sticky` 实现滚动时固定在可视区域左侧

**涉及文件**：`css/level-select.css`（`.scroll-to-current-btn` 样式）

---

## 问题 24：教程卡片出现在屏幕左上角（无任何定位）

### 现象
教程的多个步骤中，卡片出现在整个屏幕的左上角（0, 0 位置附近），完全没有居中或靠近目标元素。受影响的步骤包括：
1. 步骤 0「欢迎来到记忆配对游戏」
2. 步骤 5「配对失败会怎样？」
3. 步骤 11「返回常规模式」
4. 步骤 12「开始游戏！」

所有不依赖 `updateTutorialGuidePositionNearElement` 动态定位的步骤都受影响。

### 原因
上一轮修改中，为了解决「CSS 类 top/bottom 同时设置导致卡片被拉伸」的问题，将所有 `.position-*` CSS 类中的定位属性（`top`、`bottom`、`left`、`right`、`transform`）全部移除，仅保留动画效果。

修改前的 CSS（以 `.position-center` 为例）：
```css
.tutorial-guide-content.position-center {
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    animation: fadeIn 0.3s ease;
}
```

修改后的 CSS：
```css
.tutorial-guide-content.position-center {
    animation: fadeIn 0.3s ease;  /* 仅保留动画，无任何定位属性 */
}
```

但是，对于 `intro`、`show-mismatch-result`、`free-play` 等步骤，定位方式是调用 `updateTutorialGuidePosition('center')` —— 该函数**只通过添加 CSS 类来设置位置**，完全依赖 CSS 类的定位属性。当 CSS 类的定位属性被移除后，这些步骤的卡片就没有任何定位信息了，默认停留在 DOM 初始位置（左上角）。

两类定位方式对比：
- 调用 `updateTutorialGuidePositionNearElement()` 的步骤：通过 JS inline style 设置 `top`/`left`/`transform`，不受 CSS 类移除影响 → ✅ 正常显示
- 调用 `updateTutorialGuidePosition('center')` 的步骤：完全依赖 CSS 类定位，CSS 类定位被移除后 → ❌ 左上角

**根本原因**：修复 CSS 类与 JS inline style 的定位冲突时，采用了「全部移除 CSS 类定位」的方案，但没有考虑到**部分步骤只依赖 CSS 类定位**（不调用 JS 动态定位函数），导致这些步骤失去所有定位信息。

### 触发方式
1. 启动游戏 → 进入新手教程
2. 步骤 0（欢迎界面）直接出现在左上角
3. 或翻牌配对失败后，步骤 5（配对失败会怎样）出现在左上角

### 涉及文件
- `css/tutorial.css`（`.position-center`、`.position-top`、`.position-bottom` 等类的定位属性被移除）
- `js/tutorial.js`（`updateTutorialGuidePosition` 函数只设置 CSS 类，未设置 inline style）

---

## 问题 25：「体验限时模式」指引没有固定在对应按键，位置偏移较大

### 现象
教程进行到「体验限时模式」步骤（步骤 9，`click-timed-mode`）时，教程卡片没有紧挨着顶部高亮的「限时模式」按钮，有时会出现相当大的偏移，位置不稳定。

### 原因
这是一个多因素共同导致的问题：

#### 原因 1：`gameBoard` 参考位置引入计算干扰（最主要原因）
上一轮修改中，为了让教程卡片「放在第一行卡牌的上方」，在 `position === 'top'` 的计算逻辑中加入了 `gameBoard` 作为参考：

```javascript
const boardRect = gameBoard ? gameBoard.getBoundingClientRect() : rect;
const safeTop = Math.max(30, boardRect.top - 180);
const elementTop = rect.top - arrowHeight - cardExtraSpace - 140;
content.style.top = Math.min(safeTop, elementTop) + 'px';
```

这段代码的设计初衷是针对**游戏界面**的：确保卡片在游戏面板（`gameBoard`）上方，不遮挡卡牌。

但是，当教程处于**关卡选择界面**时（如 `click-timed-mode` 步骤）：
- `gameBoard` 元素仍然存在于 DOM 中，但游戏界面是隐藏的（`display: none` 或 `hidden` 类）
- 隐藏元素的 `getBoundingClientRect()` 返回值不可靠（可能是 `0, 0, 0, 0`，也可能是元素本来该在的位置）
- `boardRect.top - 180` 会计算出一个完全错误的值
- `Math.min(safeTop, elementTop)` 会选择这个错误的值，导致卡片位置严重偏移

简单来说：`gameBoard` 参考系只适用于**游戏界面**的步骤，在**关卡选择界面**的步骤中使用会产生干扰。

#### 原因 2：顶部元素使用 'top' 定位方向不合理
`levelModeTimedBtn`（限时模式按钮）位于页面顶部，使用 `position: 'top'`（卡片在元素上方）意味着卡片要放到视口最顶部以上的区域，空间非常有限，很容易顶到视口边缘。

对于页面顶部的元素，合理的定位方向应该是 `'bottom'`（卡片在元素下方），这样有充足的空间展示卡片。

当前代码中：
- `showTutorialArrow(levelModeTimedBtn, 'bottom')` —— 箭头方向是 bottom
- 但 `updateTutorialGuidePositionNearElement` 默认或优先选择 'top' 定位
- 方向不匹配也可能导致位置计算混乱

#### 原因 3：滚动后位置不更新
关卡选择界面是可滚动的（`level-select-map` 很长），如果用户在教程步骤中滚动页面，目标按钮的 `getBoundingClientRect()` 会变化，但教程卡片的位置是在步骤切换时计算一次，之后不会自动更新，导致滚动后偏移。

### 触发方式
1. 启动游戏 → 进入新手教程
2. 翻到「限时模式介绍」步骤，点击「下一步」跳转到关卡选择界面
3. 观察「体验限时模式」卡片与限时模式按钮的相对位置，可能出现明显偏移
4. 尝试滚动页面，卡片位置不跟随按钮移动，偏移进一步加大

### 涉及文件
- `js/tutorial.js`（`updateTutorialGuidePositionNearElement` 函数中 top 定位的 `gameBoard` 参考逻辑）
- `js/tutorial.js`（`executeTutorialAction` 中 `click-timed-mode` 动作的定位方向选择）
